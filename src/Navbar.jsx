
const Navbar = () => {
    return (
        <nav>
            <div>
                Logo
            </div>

            <div className="login-section">
                <button>Login</button>
            </div>
        </nav>
    )
}

export default Navbar